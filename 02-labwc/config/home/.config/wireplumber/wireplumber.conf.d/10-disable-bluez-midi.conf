# Disable the Bluetooth MIDI monitor only.
# This keeps normal BlueZ audio support while avoiding the crashing
# bluez-midi registration path seen on some Debian systems.
wireplumber.profiles = {
  main = {
    monitor.bluez-midi = disabled
  }
}
